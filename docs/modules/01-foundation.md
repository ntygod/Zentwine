# ZT01｜工程底座与持续集成

状态：InProgress。负责人职责：技术负责人、平台工程、质量。依赖模块：无。对应蓝图：17、24。ZT01-01/02/03/04/05 的实际完成与证据见 [实施状态](../tasks/status.md)；本模块其余工作包仍按依赖推进。

## 目标与边界

建立所有团队共享的可构建、可验证、可追踪工程。不是在此实现业务功能；但任何后续模块不得另建独立身份、错误协议或未锁定依赖。

## 设计与落点

目录按架构总纲，packages/contracts 是共享 schema 源；domain 不依赖 UI、数据库或供应商 SDK。前端通过 client 调用 API。环境分开发、测试、预发和生产，示例变量仅有占位，不包含真实 Key。

统一 format、lint、typecheck、test、build、contract-check、migration-check、docs-check 是本模块目标；已实现命令以根 README 和 package.json 为准，其余仍待相应工作包实现。版本清单记录 Node、包管理器、数据库、SDK、容器镜像摘要及支持系统。

## 开发工作包

| ID | 前置 | 开发内容与交付 | 验收标准 |
|---|---|---|---|
| ZT01-01 | 无 | 核对稳定版本和许可，完成 ADR-001；创建技术清单、最小 Temporal/数据库 POC 与决定记录 | POC 含等待、故障重启、重复请求；采用/拒绝依据可复查，未批准不标 Accepted |
| ZT01-02 | ZT01-01 | 初始化 pnpm workspace、TS 配置、包依赖约束、锁文件、apps/services/packages 空骨架 | 全新 checkout 按 README 可安装；禁止 domain 反向引用应用；构建不需要生产凭据 |
| ZT01-03 | ZT01-02 | 实现 config/env 校验、结构化错误、trace、时间/ID 服务与脱敏基础库 | 缺变量明确失败；错误不回显密钥；固定时钟可测试 |
| ZT01-04 | ZT01-02 | 建立单元/集成/浏览器测试框架、FakeRuntime、租户 Fixture 和临时数据库 | 测试相互隔离、可并行；Fake 数据有标记，不误触付费供应商 |
| ZT01-05 | ZT01-03、ZT01-04 | 配置 CI：检查、契约、迁移、依赖和文档校验，构建制品写入摘要 | 删必需测试或提交凭据会失败；没有 live key 时明确 skipped_live |
| ZT01-06 | ZT01-05 | 编写开发环境 Compose、诊断脚本、贡献指南和可重复故障测试入口 | 新环境启动/清理可复现；退出无孤儿进程；版本/许可清单完整 |

## 测试与交付

必须测试空环境、错误配置、不同操作系统路径、断网依赖安装、CI 无秘密条件。程序输出清楚区分缺配置和产品失败。工程质量门禁不能因模型生成代码而变宽。

整个模块完成条件：构建及 Fake 测试证据、ADR、开发说明、CI 记录和依赖锁均存在，且所有本模块工作包已验收。部署业务产品不属于本模块完成声明。
