# ZT02-05验证记录

状态：Done（限定实现验收）。2026-09-25，PR #24 的修复代码已通过全部必需CI；本次仅更新验收文档，最终文档提交仍须完整复验，精确提交与合并结果以PR #24为准。不进入ZT02-06。

## 精确验证快照

输入main `0655247417c1750d0c1111b8455919a9ddf61405`；Issue #23。

代码head `355c22334144696f02876b185b8ce539ef53bab7`；被测合并候选 `2d5ae041cfd9f466e152312460b617194c31b49d`；源码tree `422e89695f6aa37e4f668126dc47de78c96496d8`。

[完整CI 36147174629](https://github.com/ntygod/Zentwine/actions/runs/36147174629) 六项任务全部success：policy、engineering / validate、database / postgres、durability / experiment、documentation / validate、quality-gate。不是将旧head的成功任务拼接到新head。

目标环境为Node 24.21.0、pnpm 11.10.0冻结安装、PostgreSQL 17.11与实际Chromium。主工程锁SHA-256 `d2717174929acfbf9eb9dc7b0c171eeb5ba0fa5e3f33eb80b3bde41425f5c7fb`，收尾修复未改依赖锁、0005迁移、历史迁移、CI门禁或原有受保护断言。

## 实际回归结果

| 验证范围 | 结果 | 核验内容 |
|---|---|---|
| 主工程 | 279/279 | 构建、类型检查、lint、格式检查通过；含新增2项CONNECT SQL/注入回归 |
| 组织生命周期、联邦身份适配、HTTP/WS | 53/53 | 设置CAS、成员/最后负责人、邀请并发/重放/撤销/到期、访客分享与deny、组织会话、供应商版本及停用传播、锁等待与事务回滚 |
| 组织真实浏览器＋PG | 3/3 | 负责人实际保存设置/连接；访客接受邀请/搜索/撤销；双窗口组织版本与390px窄屏 |
| 原有工作台与独立Studio浏览器 | 9/9 | 保留旧浏览器场景，不由新页面测试替代 |
| Agent委派 | 51/51 | 实际PG、HTTP/工具与委派链/预算 |
| 审批 | 49/49 | 版本绑定、撤销通知与故障场景 |
| 授权策略与WebSocket | 38/38 | 实际数据库与逐消息复核 |
| 持久身份 | 28/28 | 身份/租户边界 |
| Fixture与数据库隔离 | 27/27、13/13 | Compose重复13/13；不计为新增独立场景 |
| 迁移与开发环境 | 8/8、8/8 | 五项迁移清单检查、实际迁移演练、独立环境恢复；统一数据库演练重复13+8并清理 |
| 启动/端口冲突/取消 | 3/3 | 浏览器后4100、5173、5174端口释放 |
| 持久性实验 | 8/8单元、10/10集成 | 实际PostgreSQL＋Temporal故障；统一演练重复8+10并清理 |
| 质量与契约 | 50/50、68/68 | 11项Node＋39项Python门禁自测；6正例＋62负例 |

以上执行的测试套件均无失败、取消、跳过或todo。缺数据库配置的入口按预期非零退出，不被当作集成通过。真实模型核验单独记录为`skipped_live`、`executed=false`，不混入上述成功结论。冻结/离线安装、源码不变、文档校验及依赖审计通过；审计仅为该次依赖快照，不是永久安全保证。

## 浏览器制品检查

从数据库制品解包并逐张查看`organization-ui/owner-settings.png`、`guest-shares.png`和`member-mobile.png`。负责人页显示实际保存后的组织名和禁用的供应商配置；访客页只显示共享目录且没有管理表单；390px成员页正常换行，无横向溢出。自动化还验证会话撤销后结果清空、旧窗口版本被拒绝及HttpOnly Cookie。三项场景均连接真实API与PG，不使用路由模拟；截图不是另画的设计稿。

## 制品完整性

下载下列五份CI制品，核对GitHub返回的归档SHA-256，并逐项验证manifest内91个文件的长度与SHA-256。五份manifest均绑定上述合并候选和源码tree，`job_status=success`；源码包与日志一并保留。摘要是完整性索引，不等于独立签名或生产认证。

| 制品 | Artifact ID | 归档SHA-256 |
|---|---|---|
| 数据库 | 10869953354 | `caa87a3cf6f4e27249a6ae144e78188c92797ea26a6940ad4bb66c4fafea8add` |
| 工程 | 10869708135 | `b372cee53f9ebf27f2784c9874e5a9146e47fc0c0f34ac41ccd19a11d5830980` |
| 持久性 | 10869758060 | `1fc951a04b9920b65c6ebf87365d46cbb54caba10b38330a2634867e0ccf7f84` |
| 质量 | 10870526528 | `b30030a2cf2b86307b1139b5c00f01dee27717c88bf303d55b10ad56ec7da1ae` |
| 文档 | 10869826889 | `e801c45ee88fe8a1604c722d902aa3b191323199a0bfa505e62759627696eaba` |

## 失败记录与修复

首轮CI `36144385982` 的数据库任务 `108102002087` 在53项组织集成测试前置连接时失败：专用组织管理角色只有schema/table授权，缺少临时数据库CONNECT。随后组织浏览器、迁移和开发环境回归被跳过，未据此合并。

修复 `62f380e77425c6bb1419f46e66e4d14db560268e` 改共用`organizationGrantSql`，仅使用数据库端`current_database()`及标识符转义授予该管理角色CONNECT，保留PUBLIC拒绝和CREATE/TEMP拒绝。实际安装命令与真实PG Fixture共用该路径，Fixture新增有效ACL断言；新增SQL生成/角色注入回归。未开放数据库通用访问。

第二轮CI `36146663190`（head `62f380e77425c6bb1419f46e66e4d14db560268e`，合并候选 `b88639fdea4869464b61c675cfae182c01a10639`）确认CONNECT及有效最小ACL断言通过，组织集成52/53。唯一失败是新测试试图对从未入组的人写入资源deny，违反既有成员外键，尚未执行到接受邀请。

修复 `355c22334144696f02876b185b8ce539ef53bab7` 改为已撤销旧成员的合法deny再邀请，保留接受失败、邀请pending、无active成员的断言，并额外核对完整成员状态/版本回滚和deny不被撤销；不删除外键、不弱化deny优先规则。第三轮上述全量CI通过，失败制品仍作为历史证据保留。

## 验收边界

SSO verifier为显式合成端口测试，未内置外部IdP验证器；机器接口是规范化同步端口，不是完整OIDC/SCIM线协议或企业IdP认证。邀请仅面向已登记身份、不发送邮件；搜索限已实现的资源目录。没有真实模型调用、客户数据、付费服务或生产部署。

局部授权和角色限制不是管理员SQL沙箱。网络中断不撤销已提交事务；只承诺最后事务内检查，不声称网络送达时硬实时撤权。生命周期事件不等于完整审计账本。0005有损down仅用于销毁开发数据，不作为生产无损回滚承诺。

按既有授权实施与自审，不冒称独立第三方审核；文档提交后不得新增未经验证的代码，最终head/base、被测tree与合并SHA须在PR #24再次核对。
