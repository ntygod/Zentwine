# ZT02-05验证记录

状态：待目标运行环境精确复验。输入main@0655247417c1750d0c1111b8455919a9ddf61405；Issue #23。不得由本地检查推断真实IdP或生产认证完成。

## 范围

本地Node22完成相关TS编译和277主工程场景；35新增规则/HTTP负例。计划真实PostgreSQL覆盖组织设置CAS、不同组织会话、最后负责人、邀请消费/重放/撤销、访客搜索/到期、IdP配置版本与停用传播、事务失败/锁等待过期；另包含实际TCP WebSocket逐消息复核与规范化供应商HTTP。

三项实际Chromium+PG场景覆盖负责人设置/连接配置、访客接受邀请/搜索/撤销、双窗口组织版本/窄屏。没有使用真实企业IdP或模型。旧9项浏览器、51 Agent、49审批、38策略/WS、28身份、13隔离、8迁移及Temporal回归保留。

## 边界

SSO verifier为显式合成端口测试；机器接口不是完整SCIM线协议。局部授权和角色限制不是管理员SQL沙箱。网络中断不撤销已提交事务；只承诺最后事务内检查，不声称网络送达时硬实时撤权。生命周期事件不等于完整审计账本。

最终精确head、被测合并候选、tree、CI和制品摘要在PR核验后记录。失败须修复并重新运行，不删除旧测试或放宽安全拒绝。

## PR #24 首轮失败修复

首轮CI `36144385982` 的数据库任务 `108102002087` 在53项组织集成测试前置连接时失败：专用组织管理角色只有schema/table授权，缺少临时数据库CONNECT。随后组织浏览器、迁移和开发环境回归被跳过，不能据此合并。其余工程、文档、持久性任务成功。

修复共用的`organizationGrantSql`，仅使用数据库端`current_database()`及标识符转义授予该管理角色CONNECT，保留PUBLIC拒绝和CREATE/TEMP拒绝。真实PG Fixture新增有效ACL断言，覆盖集成和浏览器路径；新增SQL生成/角色注入回归。依赖锁、CI门禁、旧测试和0005迁移内容不变。修复后仍须对新head完成全部CI及制品核验，本段不宣称复验通过。

第二轮CI `36146663190`（head `62f380e77425c6bb1419f46e66e4d14db560268e`，合并候选 `b88639fdea4869464b61c675cfae182c01a10639`）确认CONNECT修复及有效最小ACL断言通过，组织集成52/53。唯一失败是新测试试图对从未入组的人写入资源deny，违反既有成员外键，尚未执行到接受邀请。改为已撤销旧成员的合法deny再邀请，保留接受失败、邀请pending、无active成员的断言，并额外核对完整成员状态/版本回滚和deny不被撤销；不删除外键、不弱化deny优先规则。浏览器及后续数据库回归仍须新提交复验。
