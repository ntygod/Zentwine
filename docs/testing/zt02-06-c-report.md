# ZT02-06-C 验证记录

状态：InProgress；实现尚待目标环境完整CI。基线主线 `48f5ae01f605fc5af493f39c46206064c5c854d7`，来源PR #27；此前报告中的另一个B合并SHA为笔误，已在Issue #25纠正。

本机Node22、已安装TS编译器只用于定向类型和纯函数验证，不冒充冻结Node24/pnpm安装或真实数据库、浏览器结果。最终PR记录精确head/base/被测tree、CI、实际场景数量、失败与修复历史及制品摘要；全部必要检查与制品核验成功后才允许合并。

本增量涵盖持续组织成员应急阻断、显式解除、双版本确认与幂等回执、统一权限检查、事务审批/邀请撤销与两个新的生命周期事件，含实际工作台入口。B的开发/ADR/任务状态文档批次没有重试，相关欠项保持。ZT02-06整体和ZT03不因本记录预先完成。

## 检查重点

真实场景要求合法正例后验证拒绝；覆盖跨组织不受影响、新旧会话、父子Agent、审批/通知、邀请、上游同步、角色更新、最后可用负责人、同请求并发、旧回执重放、失效操作人、事务回滚、真实数据库锁等待及拒绝删除应急历史的down。页面连接实际API与临时PG，验证手工身份确认、窄屏、跨窗口组织切换及失败后显式同请求重试。

文件/预览/导出仍属未实现业务，仅沿用A预留入口防护；严格模式退出2仍代表范围不完整。无真实模型/IdP调用、生产数据、购买服务或生产部署；不是独立第三方审核。
