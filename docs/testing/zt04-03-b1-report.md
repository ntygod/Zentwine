# ZT04-03-B1 验证记录

候选待目标完整CI。基线main `76eb4582d68731b4ba37dca8ed7e6af3f09463b4`（PR41），Issue #42。

本地Node22/TypeScript5.8.3：新契约和客户端编译、30项合成响应单测通过；其中包含严格状态/归因/回执、实际绑定摘要重算、只读恢复、精确命令、防双击、同请求恢复、失效上下文、丢失许可与执行响应、暂停抑制后续执行。不以本地合成传输结果代替真实数据库或目标工具链验收。

新增12项真实Chromium+PostgreSQL场景，覆盖双人闭环、拒绝、并发版本变化、到期、访客/跨组织、许可与执行提交后丢响应、真实存储授权故障回滚、双窗口失效、离线清输入、三主题/窄屏/键盘与不存在ID。新增2项默认浏览器未启用与非法路由场景，全部旧测试保留。这些场景当前已编写，尚未宣称执行成功。

不修改后端权限、八项迁移与manifest、迁移校验器、CI工作流或依赖锁。仅向已有组织浏览器runner追加新测试。目标CI必须完整运行工程、数据库、浏览器、持久性、文档与质量门禁，再核验制品/源码/实拍；失败历史另行追加，不删除断言或放宽必要约束。

仅目录改名审批；不代表通用内容CAS解阻、正文/证据/活动/对话接入。无业务库迁移、生产部署、模型/IdP或付费动作。最终精确head、CI、制品与合并结果见对应PR。

## 首轮失败与修复候选

初始head `f8cbe122a7c6198c7554bea1a4f88b354fc00b3b`、candidate `d8f2d4d1b5f6bebfaf87c8a3a7f152b16fd507da`、tree `db0d71f1dc810944172a57ec1c0c13cb26c1deca`，CI `36246681974`。462主工程及23默认浏览器通过，组织真实浏览器31/33（旧21全过，新10/12）。过期fixture设置为59分钟有效窗，违反既有15分钟约束，未到业务断言；现改为已过期的9分钟合法窗并增加数据库断言，不改约束。

原始断连接发生在响应头发送前，执行端收到2个HTTP请求而测试预期1；该情况与Chromium复用连接且未收到响应头时的ShouldResendRequest逻辑一致，不能把网络层重传等同于应用再次决策。修复将原丢响应场景明确为发送真实响应头后截断真实响应体，原请求次数和单回执断言保留；新增第13项独立的响应头前断连接场景，观察但不改变真实fetch，分别记录应用执行调用、HTTP到达与数据库单回执/单版本增量。未模拟授权结果或修改后端。

本机辅助HTTP诊断因浏览器返回ERR_BLOCKED_BY_ADMINISTRATOR未执行，没有更改浏览器策略或绕过本地限制，不能作为重传证据。实际传输分层结果必须由既有授权CI环境的新真实PG场景确认。参考Chromium官方源码 `net/http/http_network_transaction.cc` 的ShouldResendRequest；最终结果将追加。

首轮五份归档和源码的完整性核验不能替代失败结论，数据库job_status=failure；下游审计/应急浏览器、迁移和环境未由此轮验收。修复保留所有原场景，等待新head完整CI。
