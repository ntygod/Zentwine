# ZT04-03-B1 验证记录

候选待目标完整CI。基线main `76eb4582d68731b4ba37dca8ed7e6af3f09463b4`（PR41），Issue #42。

本地Node22/TypeScript5.8.3：新契约和客户端编译、30项合成响应单测通过；其中包含严格状态/归因/回执、实际绑定摘要重算、只读恢复、精确命令、防双击、同请求恢复、失效上下文、丢失许可与执行响应、暂停抑制后续执行。不以本地合成传输结果代替真实数据库或目标工具链验收。

新增12项真实Chromium+PostgreSQL场景，覆盖双人闭环、拒绝、并发版本变化、到期、访客/跨组织、许可与执行提交后丢响应、真实存储授权故障回滚、双窗口失效、离线清输入、三主题/窄屏/键盘与不存在ID。新增2项默认浏览器未启用与非法路由场景，全部旧测试保留。这些场景当前已编写，尚未宣称执行成功。

不修改后端权限、八项迁移与manifest、迁移校验器、CI工作流或依赖锁。仅向已有组织浏览器runner追加新测试。目标CI必须完整运行工程、数据库、浏览器、持久性、文档与质量门禁，再核验制品/源码/实拍；失败历史另行追加，不删除断言或放宽必要约束。

仅目录改名审批；不代表通用内容CAS解阻、正文/证据/活动/对话接入。无业务库迁移、生产部署、模型/IdP或付费动作。最终精确head、CI、制品与合并结果见对应PR。
