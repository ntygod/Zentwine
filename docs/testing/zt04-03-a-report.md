# ZT04-03-A 验收记录

状态：A限定代码验收通过；最后的文档提交必须独立完整复验，最终head、制品与合并见PR #41。整个ZT04-03 / Issue #40保持InProgress，B真实内容版本/证据/活动/持久批准未接通，不以A替代。

## 精确代码快照

基线main `2fa34f1480c14e90c21497b79a462bc67e44f94e`。修复head `8811add044e7385781666815ee27abf4d2564c44`，源码tree `3be610b94bf5bf371092cffd665e9df05d827262`；被测候选 `2d3e8614735925d477229de3ff9b4d86a343b483`。完整CI `36242477896`（run63、attempt1）completed/success，五个必需任务及总门禁通过。冻结Node24.21.0/pnpm11.10.0、真实PostgreSQL17.11和Chromium；本机Node22结果不替代它。

| 范围 | 实际结果 |
|---|---|
| 主工程 | 432/432（402原有＋30新契约/控制器/偏好测试） |
| 工作台/独立Studio浏览器 | 21/21（19原有＋2新默认宿主） |
| 组织真实Chromium＋PG | 21/21（12原有＋9新对象场景） |
| 审计/应急浏览器 | 各3/3 |
| 租户/组织/审计/应急PG或HTTP/WS | 34/34、53/53、28/28、26/26 |
| Agent/审批/策略/身份 | 51/51、49/49、38/38、28/28 |
| Fixture/数据库隔离/迁移/环境/启动取消 | 27/27、13/13、8/8、8/8、3/3 |
| Temporal | 8单元＋10集成 |
| 质量与契约 | 11Node＋39Python；6正例＋62负例 |
| 权限矩阵 | 24/24，12真实目录授权与12预留路径防护分开 |

执行套件无失败、取消、skip、todo；新30/2/9测试名与通过日志逐项匹配。Compose、统一数据库/持久性演练和清理通过，重复运行不计新增独立覆盖。正常/严格矩阵24个精确ID、唯一TAP计划、定义/锁/TAP摘要已核对；严格模式对尚未实现文件/预览/导出返回2，缺配置报告failed、executed=false；live独立skipped_live、executed=false，不包装为真实模型通过。A矩阵remaining_deliverables是历史快照，不代表本次B/C状态。

格式、lint、构建、类型、冻结/离线安装、文档、八迁移及依赖检查通过。原60保护文件/698声明、所有原测试、服务端权限、八项历史迁移及manifest、两个迁移校验器、CI工作流和冻结锁不变。锁SHA-256 `d2717174929acfbf9eb9dc7b0c171eeb5ba0fa5e3f33eb80b3bde41425f5c7fb`。

## 真实页面与范围

九项新PG浏览器场景覆盖目录点击/深链/刷新、真实共享资源与访客/跨组织/不存在拒绝、抽屉键盘循环/Escape/焦点返回、布局明确保存/刷新/重置/坏值/存储拒绝、撤权关闭已打开抽屉、离线及真实数据库读取权限故障恢复、双窗口上下文变化、三主题/390与320窄屏/增大根字号/强制配色。没有模拟授权API；存储故障只模拟浏览器Storage边界。

浅/深/高对比、390px资源页及读取判定抽屉五张实拍已逐张查看；字段与按钮可读，窄屏纵向排列，没有横向溢出。全页抽屉截图含滚动区域上下的背景，不代表模态内背景可交互；键盘与关闭行为另由实际断言验证。这些场景不代表所有辅助技术或跨浏览器独立认证。

对象页只展示当前服务器返回的目录元数据和读取判定。资源目录版本不是内容修订，读取不是批准、签名或执行许可。版本历史、证据、对象活动、协作对话和持久内容批准明确未接通，不显示虚构数据、零记录结论或批准按钮。

读取、抽屉和布局不提交业务命令；共享页面仅保留已有的显式组织切换及退出命令。布局保存只含schema/density/inspector固定枚举，默认不写入；不存身份/组织/资源/正文/查询/凭据/权限。Storage拒绝明确降级。按次重读并清空受保护状态，不声称实时撤回已返回数据。

## 制品完整性

五份代码归档SHA-256与GitHub元数据一致；127个manifest条目逐项核对字节数/摘要及candidate/tree/run/attempt/job_status，三份完整源码各431文件的路径/Git blob/执行位与代码tree一致。摘要是完整性索引，不是独立认证。最终文档提交的制品另记在PR #41，不复用本表作为最终验收。

| 制品 | Artifact ID | ZIP SHA-256 |
|---|---|---|
| policy | 10906730968 | `f750f84d3751990cccf2d113d83c231424df62541433618250c07caa88098e2a` |
| documentation | 10905848813 | `2f079d7d6f417f48d540eafb2cab07b1ddeca09ded7846054003315f77adb7d0` |
| engineering | 10906411339 | `c3817b0243e185db6c3ab2675f7265c5673ef801e4cc200028f3685f9e7a0521` |
| durability | 10906776636 | `e2516137f24985fd245fe60a98cac28a45cf52f5bf3de887be565e756b1b4e1a` |
| database | 10906057971 | `f1b25707fa23b783234673a78abd254e20235e77c43c78604b5975681edf4755` |

## 首轮失败与修复

初始head `043631caf069b1f40cf1bfd1bb8f6d1b82ffe6ed`、tree `cef8326cdd56f6ae45e3f99041f2e0bb060364f6`、candidate `acac3d5db5ac008cde24d32b20cfa9def921a157`，CI `36241997813`（run62）：432主工程和21普通浏览器通过；组织浏览器19/21，原12全部通过，新对象7/9。失败一是单控件原生dialog的Tab焦点离开模态；失败二是新主题测试错误使用精确label定位，主题和窄屏主体未执行。下游审计/应急浏览器、迁移及环境验证被阻断，没有合并。

首轮五份归档的103个manifest条目和三份源码已核验完整性，但数据库job_status=failure明确保留。修复8811add增加生产抽屉正反向Tab边界循环，保留原生模态、Escape和焦点返回并避开组合输入；焦点测试断言不改。主题测试改为既有role/name定位，增加精确主题属性和真实背景色检查，没有删场景、放宽超时或修改服务端权限。修复后才由新完整CI36242477896验证成功。

本地Node22.16.0/TypeScript5.8.3定向contracts/client编译、30新＋21旧导航单测51/51及格式检查通过。最初应用直接依赖client违反包边界，已将布局交互放入UI hook；边界规则不变。本地没有真实PG或Chromium，不冒充目标环境执行。

## 未交付范围

B待真实内容版本/证据/活动/持久决策接入，批准须显示并绑定revision/hash、范围和责任人，由服务端最新权限及并发检查决定。没有安装未验证0009或合并PR #33，不重试被拦截的迁移校验修改。Issue #25/#32/#35保留，未推进ZT03-03或ZT04-04。无业务库迁移、生产部署、真实模型/IdP或付费动作；授权实施及自审非独立第三方审核。
