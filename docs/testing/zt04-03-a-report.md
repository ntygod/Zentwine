# ZT04-03-A 验收记录

状态：候选待验证。基线main `2fa34f1480c14e90c21497b79a462bc67e44f94e`。Issue #40保持完整工作包范围，A只交付目录对象页/读取判定及外观偏好，不完成B的真实内容批准。

本地Node22.16.0、TypeScript5.8.3：contracts/client编译通过，30项新增单测＋原21项导航单测共51/51。固定Prettier3.6.2、包边界检查通过。本地首次边界检查发现应用直接依赖client不符合既有分层，已将布局交互封装为UI hook；规则未修改。本地没有真实PG/Chromium，不能代替目标冻结Node24/pnpm11完整CI。

目标新增9项真实Chromium+PG、2项默认宿主浏览器测试，保留原402主工程、19浏览器、12组织PG浏览器及全部旧数据库/审计/应急/身份/策略/委派/审批/迁移/环境/Temporal回归。执行结果、精确head/tree/run、失败历史与制品在PR和最终记录中登记；全部必要检查和源码/制品核验通过后才合并A。

只读路由、抽屉开关、密度与侧栏不触发业务写；唯一POST是用户明确切换组织。布局只有固定枚举可选保存，不包含受保护数据。期望不同组织/未知对象/未授权/过时会话不返回旧资源；数据库故障恢复也须重新请求。新浏览器场景没有模拟授权API。严格矩阵仍因文件/预览/导出实际业务未实现返回2，live独立未执行。
