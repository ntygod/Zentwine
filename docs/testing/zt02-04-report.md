# ZT02-04｜审批、一次性许可与撤权通知验证

基线main@570c9297369eb5ddf1e0caa9093ff76236ee47a4。最终被测commit/tree与CI证据以关联PR为准；本记录不提前宣布远程测试通过。

## 已执行本地检查

Node22.16.0离线工具环境下，相关TypeScript包编译与242个主工程测试通过（原211+31审批规则/API负例）。这不是目标Node24正式验收，真实数据库、迁移和TCP需CI补齐。

## 新增真实验证范围

真实PG隔离库：自批拒绝、生产目录批准、policy来源、重复请求/决策/许可/消费、内容与环境变更、到期、并发、持久回执、上游撤权原子通知、回滚和消息游标。多数HTTP用Fastify inject，一个流程使用真实loopback TCP。锁等待用pg_stat_activity确认，不把sleep本身当锁证据。

不修改旧测试断言、不放宽迁移检查、不新增凭据豁免。新迁移不用SECURITY DEFINER过程，撤权通知由已锁定的治理仓储同事务生成。无UI与真实模型验收，live仍单独记录。

## 边界

许可只对人类scope的catalog.rename；非公开生产认证或Agent高风险提权。审批者是不同的有资格owner；未来细粒度审批角色另行实现。通知持久轮询不是实时推送，直接管理员SQL和自然到期不保证产生通知，但消费检查仍拒绝不兼容状态。有限本机测试不能证明大规模吞吐、分布式故障恢复或第三方安全认证。
