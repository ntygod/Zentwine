# ZT02-06-B｜组织审计视图验收与文档补录

本记录补齐已合并B实现的仓库证据入口，编写基线为main `a30e7fe0257315fd7ad94c67c24a7b0772541d0f`。历史B验收、当前C兼容回归和本次文档PR验证分开记录，不把历史CI当成本次新提交通过。设计取舍见[ADR-016](../adr/ADR-016-organization-audit-view.md)，使用与恢复见[指南](../development/organization-audit.md)。

## 历史B代码验收

PR #27最终head `2ec7c5fe5a2b9dfd037804d4576bf33689ab321e`；CI `36160711431`，attempt1；被测候选 `a8e813b6e1af66ae8c409e9c28981540b751896c`；源码tree `ed2d5015c4c4a12b9854859fbf0726afcdae98f7`。实际合并提交为 `48f5ae01f605fc5af493f39c46206064c5c854d7`。此前报告中8e9f开头的另一SHA是记录错误，不作为证据。

原B交付九类组织生命周期事实投影、owner范围鉴权、bigint键集分页、绑定会话的15分钟游标、0006迁移及实际页面。历史结果：313主工程（含19项新规则/游标/解码测试）、28项审计PG/HTTP、3项审计Chromium+PG，以及原有全部回归通过；工程、数据库、持久性、质量、文档和总门禁成功。文档任务当时只验证旧文档，不代表B使用指南或ADR已经写入。

初轮CI `36158333416`的数据库与新页面场景通过，但工程格式失败，未合并。固定格式修正后补充了两组织交错历史分页，并将游标篡改改为实际字节翻转；最终head完整复验成功。没有放宽生产权限、删除旧断言或变更历史迁移和冻结依赖。

## B测试覆盖映射

| 证据文件 | 已验证边界 |
|---|---|
| tests/organization-audit.test.mjs | 精确查询规则、游标篡改/到期/绑定、实例密钥、严格响应解码与跨组织响应拒绝 |
| tests/audit/persistence.test.mjs | 空历史、旧事实迁移、仓储重建、同时间戳及大整数分页、两组织交错、owner资格、访客隔离、退出/降权/停用/会话截止、来源连接归因、事务回滚、历史改写拒绝、真实锁等待及读取后到期重查 |
| tests/audit/http.test.mjs | 真实TCP读取、no-store、固定字段、跨组织与未授权请求、查询校验、无修改/删除接口、存储失败及默认禁用模式 |
| tests/audit/browser.test.mjs | 实际API+PG筛选与20/3分页、桌面/390px、双窗口组织变化拒绝、故障清空和撤权 |

数据库测试与页面都使用专用可销毁PG环境，页面没有路由模拟；供应商身份事件来自本地合成适配调用，不代表真实IdP认证。测试数量按最终日志计，循环生成场景与重复演练不重复宣传为新覆盖。

## 历史B制品定位

历史五份归档及104个manifest文件条目、三份各354文件源码的校验结果记录在PR #27；以下为完整性索引，不是独立审计签名。CI制品有保留期，过期后应重新执行受控验证，不可伪造缺失原始日志。

| 制品 | Artifact ID | SHA-256 |
|---|---|---|
| 数据库 | 10875437211 | 946c85bd5e20869ef7967fec2eb20f9f8a48eadab7bb215b47c326dd44aa4230 |
| 工程 | 10874509972 | 3366f7fb998997c71de0acd2be3c06c1c69f18e9663f2d34cc7faa8d44d31f3b |
| 持久性 | 10875561287 | fd4c0d89fe07ae0830cbde8a074ab9e26312f28f4b1147f77fe1c5ac86999122 |
| 质量 | 10875143965 | 6aa729eddfcd3322ea40aebee48d7f753786ba0b2f09873baab169fcaf728b5c |
| 文档 | 10875099325 | fab64fbff6ebf494154bbd9422e113cbe325984acc3371e582cdfc2a198ed122 |

## 当前主线兼容回归

C的PR #28已新增两个应急事件，当前视图有十一种已记录事件，B原九类语义保留。基线main的源码tree为 `dd94b6008a15c4c04bb4c31263c0d43143f48991`，与PR #28最终候选 `b102b4466062588daa98233a39c3612a053fb1be`相同。CI `36212531304`包含329主工程、原28审计PG/HTTP、3审计浏览器、26应急接口/持久性、3应急浏览器及其余回归；具体见[C记录](zt02-06-c-report.md)和PR #28的最终文档提交验证。

本轮从该已核验归档提取368文件并重建Git树，得到相同tree，未依据过往无法读取的d49/f2f提交。C历史错误与撤回说明保留，不用本次文档补录覆写失败历史。

## 本次文档变更门禁

新增B指南、ADR、此记录和全通道后续验收追踪；同步任务/模块/导航中仍停留在A阶段的措辞。仅文档变更，不更改API、UI、权限、迁移、依赖、矩阵定义、既有测试或CI工作流。原A/C验收报告保持历史快照。

文档合并必须以本次PR实际head/base及新完整CI为准。核对五个必需任务与总门禁、数据库和浏览器日志、归档摘要、manifest及被测源码树；失败时修复并重新运行。最终运行ID、制品及合并SHA记录在本次PR中，避免为回填本文件产生无穷自引用提交。没有本次CI结果前，此记录只证明历史事实和拟提交文档内容。

## 尚未满足的完整验收

补齐B文档不实现文件内容、隔离预览或导出。ZT02-06原始六入口验收标准保留，三个实际业务通道仍需各自真实正例、越权反例、降权、旧授权及并发撤销；当前严格矩阵仍退出2。见[待办追踪](../tasks/zt02-06-acceptance-followup.md)。

当前审计不是完整账本、匿名化数据、不可篡改认证或生产发布；不覆盖通用对象历史、完整执行/审批/登录轨迹、保留清理或管理员直接SQL。此次没有迁移业务库、生产部署、真实模型/IdP调用或付费操作。依据项目授权实施与自审，不声称独立第三方审核。
