# ZT02-06-A 验证记录

状态：待目标环境CI核验。输入main `7c7a7f3672f8617a0bb0886d55ab3e48660df0cc`，Issue #25；本轮仅权限矩阵与策略回归增量，不是整个ZT02-06交付。

本机Node22的纯验证器自测及新增MJS语法检查已执行。初次自测发现TAP过滤器把`# todo 0`汇总行误当TODO指令；已修复过滤逻辑，保留正例及skip/todo负例覆盖，并补充TAP头和唯一完整plan校验。目标Node24.21.0、pnpm11.10.0冻结安装、真实PG17.11、HTTP/WS及所有原有回归未由本地环境执行，不冒称完成。

计划核验24个矩阵场景：12个现有目录功能授权场景，12个未实现入口默认拒绝场景。每个场景要求已知真实HTTP正例；三类现有入口验证跨租户、降权、缓存旧allow与真实锁等待并发撤销。浏览器代码未修改，旧9项浏览器与组织3项Chromium+PG仍须完整回归。

CI制品必须同时保存矩阵定义/源提交/源码tree/锁和TAP摘要、场景ID及缺配置非零证据。不能用旧PR绿色结果替代本次精确head验证。没有新数据库迁移，没有减少历史测试、约束或凭据扫描规则。

最终执行数量、制品核验和合并提交写入本PR验收说明；文档追加后同样重新验证最终head。文件/预览/导出仍未实现，审计界面和应急撤权流程留在Issue #25，ZT02-06保持InProgress。

## 首轮CI失败与修正

首轮run `36151182846`，head `505916eb05606bf020c77aed35e210735c054e36`，被测候选 `15ad7fc758b1c056872bbb2d323f47ca60c5a1b2`，tree `1ddd8b6395a04a9f568ed054952140a561d8fee8`。policy、文档和持久性成功不能代替全量通过；工程被新文件格式阻断，数据库矩阵18/24，后续组织浏览器/迁移/环境步骤未执行，不据此合并。

六个失败来自两处新测试错误，各影响REST/WS/搜索：隐藏资源使用了未定义的`private`枚举，已改为既有`restricted`并指定Alice为所有者，额外验证Alice确实能读再验证Bob不能读；撤权后的受保护`evaluate`会先拒绝不可读资源，已按既有契约断言`unavailable_resource`异常，不要求泄露资源的普通deny返回。不修改生产权限逻辑或历史断言。首次并发撤销三种真实通道和12个预留入口防护已通过，仍须与修复后的其余场景在同一最终提交完整复验。

已使用固定Prettier的只读渲染修正四份新源码格式，并移除临时诊断输出。增加严格模式的真实重复运行：即便24个场景成功也须因三项通道未实现而返回2，独立保存其报告与TAP；缺配置仍返回1且不能遗留绿色报告。
