# ZT01-02 验证报告

## 范围

正式 pnpm/TypeScript 工程、真实本机只读 API、管理工作台和独立 Studio 入口。此报告不宣称需求管理、身份授权、业务持久化、代码编辑、多模型执行或生产部署完成。E01–E52 仍未通过完整产品验收。

## 冻结源码复验

- 分支提交：`3e990c22c3d74837be4d1fb1fe6554992f503109`。
- 实际 PR 合并测试提交：`b71ddf428561c08eb0516c7b7874021d025d9dab`。
- [CI 35980200109](https://github.com/ntygod/Zentwine/actions/runs/35980200109)：冻结安装、格式只检查不改写、lint/包边界、构建、严格类型、30 个核心/API 测试、7 个真实浏览器场景、环境诊断和安装依赖后的文档检查通过。
- 浏览器进程退出后，4100 / 5173 / 5174 全部可重新绑定；git diff --exit-code 通过，测试没有修改受追踪源码。
- 当次依赖审计报告 0 项已知告警，另归档依赖许可元数据；不等于永久安全或法律合规认证。
- artifact `10800090998` ZIP SHA-256：`85e9dc23aaa46532cf69b8bd229ca5c844812d3a6ad94641c7f7ff6195999118`。
- 已下载核对 artifact，确认 source.zip 中锁文件与运行所用锁逐字节一致，最终源码不含临时导入工作流。

之后的状态文档变更仍以 [PR #5 Checks](https://github.com/ntygod/Zentwine/pull/5/checks) 中其精确提交为准。原始报告保留被测提交，不将旧 SHA 改写为新 SHA。

## 首次验证与源码固定

首次通过分支 `f60f4bd664abd01b7e282ebd4b556c49cdc11cf8`，实际测试提交 `afe8a67fa19b3d6f3c8728e61ad3886d5aa2a953`，[CI 35979485829](https://github.com/ntygod/Zentwine/actions/runs/35979485829)。该次以只读权限安装并格式化初始源码，原始 Git source.zip 与格式化结果分别归档。

- artifact `10799636683` SHA-256：`e5b9c814166446d0ae8570321fcf8c5b5c94c70dcef9f97e528f8e86dbbb5dab`。
- `formatted-files.json` SHA-256：`c65b7b30c80ead419c012fa2417834bfd23f2b61267a7ad786ca6e7d70ad7509`。
- `pnpm-lock.yaml` SHA-256：`162cb3a48c9a84d506fc4e81261e52adc9e7dd4eee67e17abd7eaaf734e44691`。

47 个格式化文件与锁经本地核对，使用一次不 checkout、不执行仓库代码的数据导入工作流，仅写入本功能分支，提交 `edf65bb1cf6143f36880d5479a5fee55ab93f90e`。该临时工作流已删除；最终 CI 只有 contents:read，不回写仓库，使用 frozen-lockfile 并核对源码未变化。

## 测试内容

核心/API：非法配置、生产模式拒绝、深链输入、版本比较、固定时钟、结构化脱敏、客户端取消和网络错误、静态/动态/跨包引用边界、响应契约、未知资源统一拒绝、Host/跨站请求限制、只读重复请求和任意异常类型。

浏览器：管理端打开独立窗口；直接进入和未知工作区；网络失败与重试；不兼容协议；未知管理页面恢复；390px 窄屏；无未捕获应用异常。导航期间不发写请求。因为尚无执行系统，这不等于已验证真实 Agent 的 exactly-once。

截图在 artifact 的 screenshots/，来自真实构建页面、浏览器和 API。由本次 AI 实施者查看截图并检查布局与中文显示，未进行独立人工审核；截图不是图像生成器制作的设计图。

## 本地附加检查

下载的实际源码/格式化产物通过文档+JSON Schema 检查：30 模块、180 任务、52 场景、8 份契约 JSON，0 错误。循环依赖、缺失引用和生成目录排除负例通过。没有在本地环境重跑 Node24/浏览器，其证据来自 GitHub CI。

## 边界和失败修复

仅 Linux CI 实测；Windows 清理代码未据此获得验证。无凭据、客户代码、模型费用或云资源创建。结构化脱敏不是通用 DLP。当前应用禁止生产启动和业务执行。

初次严格类型检查发现 Node 类型范围未声明；补齐服务端配置。随后 unknown 异常类型被直接访问，改为守卫并补回归测试。格式检查发现链式表达式规范化不稳定，重写清晰分支。未删除断言或放宽类型检查。
