# ZT01-02 验证报告

## 范围与状态

正式 pnpm/TypeScript 工程、真实本机只读 API、管理工作台和独立 Studio 入口。此报告不宣称需求管理、身份授权、业务持久化、代码编辑、多模型执行或生产部署完成。E01–E52 仍未通过完整产品验收。

## 首次全流程通过的证据

- 分支提交：`f60f4bd664abd01b7e282ebd4b556c49cdc11cf8`。
- 实际 PR 合并测试提交：`afe8a67fa19b3d6f3c8728e61ad3886d5aa2a953`；与分支 SHA 明确区分。
- [CI run 35979485829](https://github.com/ntygod/Zentwine/actions/runs/35979485829)：构建、严格类型、依赖边界、30 个核心/API 测试、格式检查、7 个真实浏览器场景通过；当次依赖审计报告 0 项已知告警，不代表永久安全保证。
- 浏览器使用实际构建页面与 Fastify API，截图在 artifact 的 `screenshots/`；不是图像生成器制作的设计图。
- artifact `10799636683` ZIP SHA-256：`e5b9c814166446d0ae8570321fcf8c5b5c94c70dcef9f97e528f8e86dbbb5dab`。

## 锁定与复现

首次工作流以只读权限安装依赖、格式化初始源码后验证；原始 Git source.zip 与规范化内容分别归档。47 个规范化文件和生成锁经过本地哈希核对，通过一次不 checkout、不执行仓库代码的数据导入工作流写入本功能分支。

- `formatted-files.json` SHA-256：`c65b7b30c80ead419c012fa2417834bfd23f2b61267a7ad786ca6e7d70ad7509`。
- `pnpm-lock.yaml` SHA-256：`162cb3a48c9a84d506fc4e81261e52adc9e7dd4eee67e17abd7eaaf734e44691`。
- 数据导入提交：`edf65bb1cf6143f36880d5479a5fee55ab93f90e`。

该临时写权限工作流已从最终树删除；最终 CI 只有 contents:read，不回写仓库、不执行格式修复，只允许 `pnpm install --frozen-lockfile`。最终检查还核对测试后源码无变化、文档检查不遍历依赖目录，以及浏览器退出后 4100/5173/5174 端口全部释放。

最终精确提交与复验结果以 [PR #5 Checks](https://github.com/ntygod/Zentwine/pull/5/checks) 和对应 artifact 为准；首次通过不代替对最终提交的复验。

## 实际测试内容

核心/API 测试覆盖：非法配置、生产模式拒绝、深链输入、需求版本比较、固定时钟、错误脱敏、客户端超时/取消路径、静态/动态/跨包引用边界、响应契约、未知资源统一拒绝、Host/跨站请求限制、只读重复请求和任意异常类型。

浏览器场景：管理端打开独立窗口；直接进入和未知工作区；网络失败与重试；不兼容协议；未知管理页面恢复；390px 窄屏；无未捕获应用异常。只读导航不发起写请求。因为尚无执行系统，这不等于已验证真实 Agent 的 exactly-once 行为。

## 本地附加检查

在下载的实际源码/规范化产物上运行文档+JSON Schema 检查：30 模块、180 任务、52 场景、8 份契约 JSON，0 错误。另对循环依赖、缺失引用和生成目录排除做负例检查。这里没有本地重跑 Node24/浏览器，相关证据来自上面的 GitHub CI。

## 保留边界

仅 Linux CI 经过实际验证。Windows 清理路径存在但不能据此宣称已验证 Windows。无凭据、无客户代码、无模型费用、无云环境创建。结构化脱敏不是通用 DLP；应用仍禁止生产启动和业务执行。

## 失败与修复记录

初次严格类型检查发现 Node 类型范围未显式声明；补齐服务端类型配置。随后 Fastify 的 unknown 异常类型被直接访问，改为守卫判断并增加敏感信息回归测试。格式检查发现链式表达式规范化不稳定，重写为清晰的带块分支。没有删除失败断言或降低类型约束。
