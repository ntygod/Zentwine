# ZT01-05 验证报告

状态：待正式 CI。输入基线 main@9b7357d69a375263b33290e157293369ff5ecac5。

本地受限 Node22/Python环境的门禁单测通过，不能替代锁定 Node24/pnpm 的全工程、真实 PostgreSQL 与 Chromium 结果。最终通过条件：所有必需工作流成功；合并前核对精确 head/base、下载 artifact 并验证摘要；不得用旧成功记录替代改动后的提交。

检查器负例包括删除/改写测试、skip/only、遗漏/失败/取消依赖、漏掉实际测试命令、不固定Action、过宽权限、凭据命中与窄白名单、schema远程引用、迁移历史覆盖、损坏/缺失/增加产物、旧提交和路径穿越。新增迁移执行器用独立数据库测试事务与失败恢复。原94核心/API/Fixture、9浏览器和13数据库场景不改写。

真实模型状态单独记录 skipped_live（缺凭据）或 blocked_live（配置存在但适配器尚未实现），本任务不作任何 live 成功声明。当前业务迁移为0，合成回滚测试不等于真实产品数据库已就绪。
