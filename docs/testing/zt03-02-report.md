# ZT03-02 验证记录

状态：Blocked／Draft。代码候选已实现，但迁移门禁兼容问题未解决，完整目标环境CI尚未通过。基线 main `4cda38a41bcbf76856bc8c9a801525c9482037cd`。Issue #32及对应PR记录精确提交、失败修复、制品与合并。

## 本地已执行

本机Node22环境：domain/policy/db类型编译通过；20项新规则/规范化/摘要/事务测试与15项旧租户规则/事务测试共35项通过。固定Prettier3.6.2用于格式校验。本地并未运行真实PostgreSQL、冻结Node24环境或浏览器，不将这些定向检查当完整验收。

## 目标验收

32项新增真实PG测试接入必需数据库任务，包括双会话同版竞争、旧版本冲突、合法及非法转换、作者不能自批、负责人权限、同摘要新修订、不可变关系、租户隔离、权限误授、原始SQL拒绝、过期/撤权、锁等待、组合事务回滚及保护性降级。另保留344项旧主工程测试、34租户PG及全部原浏览器/身份/策略/Agent/审批/组织/审计/应急/环境/Temporal回归。

最终head必须从冻结安装开始完整CI；所有执行套件不得失败、skip、todo或取消。新集成入口缺配置返回1。Issue #25的未实现文件/预览/导出仍使严格矩阵退出2，不改变其验收范围。真实模型独立未执行，不混入成功。

## 限定范围

只保存服务器声明的内容描述符，不存正文、不验证外部内容存在；真实领域输入、资源授权和内容持久性由后续模块承担。内部批准状态不是执行许可、生产发布或ZT02策略审批。纯人类会话端口，无新公开API/UI或Agent/Service适配。无自动重试、事件、幂等操作对账或删除保留流程。0009有历史后拒绝删除式回退，可信迁移所有者和管理员不属于敌对隔离范围。

## 阻断与未交付内容

本地首次静态迁移检查把0009的PL/pgSQL函数内部BEGIN识别为禁止的事务控制。尝试发布词法函数块识别扩展时，写入被工具拦截；没有更换工具、路径或编码重试。仓库候选保留原Python门禁和Node迁移执行校验，不包含被拦截扩展或依赖它的新增门禁测试。

因此原迁移门禁仍会拒绝0009，不能把本地单测或此前历史提交的CI当成本工作包通过；32项真实PG及本提交完整回归尚未验证。PR只能作为Draft，不可合并；Issue #32保持打开。既有门禁的这一拒绝是预期阻断结果，不是已修复。没有创建绕过门禁的执行入口或触发额外未授权测试通道。
