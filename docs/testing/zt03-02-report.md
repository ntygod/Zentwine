# ZT03-02 验证记录

状态：InProgress／B仍Blocked。A已经通过PR #34合并；B只完成主线整合和适配层缺陷修复的候选，0009迁移兼容尚未解决。PR #33与Issue #32记录当前精确head和实际CI；不能把单元测试通过当作数据库验收。

## 原始失败及A拆分

原B head `9ba7a29aeaf58b569454a52413b2167c9b78ef07`，CI `36221177636`（run51）实际failure：policy拒绝0009函数体中的BEGIN，总门禁失败，工程/数据库/文档/持久性因前置失败跳过。原本机20新＋15租户单测35/35通过，不代表32项版本PG已执行。

质量制品10899232186、归档SHA-256 `185a0f5b755bcc15de6f28da04d7764903820f4d88284b1f73371e6f9b8496c2`，8个manifest条目已核验，job_status=failure。兼容校验器写入被工具拦截这一历史保留；本轮没有重试被拦截写入、改编码或改SQL语法来躲避检查。

A以独立PR #34合并，head `761f62b11b274a8e0e5a8c8486781f6af6496306`，完整CI `36223617006`，合并 `b7e1c4abd6a4f531e16e2dabf097dea2407e56e4`。370主工程及旧全回归通过。A没有安装0009；其中34项租户PG不能替代B的32项版本PG。

## 本轮整合与本地实际结果

保留A主线的content-digest与domain版本规则。B的versions模块兼容重导出同一摘要函数，避免旧实现绕过描述符快照；command复用A的同步深度冻结快照，不改变入参接口。数据库总入口显式导出适配器，保留原B测试的模块导入。

新增16项适配层单测验证：导出函数同一性；旧入口不执行描述符getter；异步等待前深层输入快照；合法响应；错误摘要/schema/状态/操作人、错误动作修订元组、异常谱系及重复行拒绝；历史版本精确一致；响应错误与输入错误分开；错误关系不回显原始内容；最终鉴权失败仍回滚；跨组织及替换对象响应被拒绝。

| 本地检查 | 结果与范围 |
|---|---|
| Node22.16.0／TypeScript5.8.3 | domain、policy、db类型编译通过，非冻结目标环境 |
| A原测试 | 26/26，原文件和黄金样本不变 |
| B原测试 | 20/20，文件与原draft的Git blob一致 |
| 原租户规则/事务测试 | 15/15，文件不变 |
| 新适配层测试 | 16/16，合成SQL响应，不连接真实数据库 |
| 合计 | 77/77，无失败、取消、skip或todo |
| 固定Prettier3.6.2 | 校验本轮修改的TS/MJS文件，不在CI里重写源码 |

首次本地执行中61项旧测试通过，新适配层文件因引入整个db入口而触发未安装的pg依赖，文件主体未执行。新单测改为直接测试其真实的无连接兼容模块入口，未使用pg假包或跳过测试；总入口由类型编译验证。随后完整重跑上述77项通过。这个本地环境问题不归因于生产缺陷。

## 尚未执行的验证

32项版本PG原定义保持不变：双会话同版竞争、旧版本冲突、合法/非法转换、作者不能自批、负责人权限、同摘要新修订、不可变关系、租户隔离、权限误授、原始SQL拒绝、过期/撤权、真实锁等待、组合事务回滚及保护性降级。

当前head必须通过冻结目标环境的完整CI及制品核验。0009仍受原迁移门禁拒绝，不能绕过它先运行数据库场景；本轮没有真实版本PG、浏览器或持久性验收结果。新增适配层单测也不证明数据库已正确执行锁、权限或写入。

## 兼容修复的审查边界

两份校验器仍分别是scripts/quality/gate.py和scripts/quality/migrations.mjs。维护者需要独立审查函数块语法兼容：允许明确函数声明中的结构块，仍拒绝顶层事务控制与危险命令，不能简单跳过整个函数体。必须保留文件限额、摘要、路径、历史迁移、事务与回退保护，并用Python/Node一致性正反例及实际迁移演练验证。本文仅登记审查条件，不提供绕过现有门禁的执行路径。

## 限定范围

只保存服务器声明的内容描述符，不存正文、不证明外部内容存在。领域输入、资源授权和内容持久化由后续业务负责。内部批准状态不是执行许可、生产发布或ZT02审批。无新HTTP/UI、Agent/Service适配、outbox/inbox、自动重试或幂等对账。

0009、两份迁移校验器、历史0001–0008、manifest、工作流、冻结依赖及既有测试没有因整合而放宽或移除。Issue #32和PR #33保持打开/Draft；Issue #25完整通道待办不变。无业务库迁移、生产部署、真实模型/IdP或付费操作。
