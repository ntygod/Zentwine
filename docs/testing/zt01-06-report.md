# ZT01-06 验证报告

状态：Pending CI。本文件先记录验收设计，最终以PR与被测commit精确记录为准。

## 覆盖

1. 纯工具29项：参数/配置拒绝、无外发密钥环境、Docker本地目标、进程组停止、锁互斥、模拟启动/清理/拒绝/重试、诊断和损坏工具拒绝。
2. 真实Docker/PostgreSQL环境：独立启动与复用、两环境并行、异常清理、身份功能不被自动打开、daemon不匹配拒删、CLI往返。
3. 真实页面生命周期：production/参数拒绝、端口冲突不杀陌生服务、取消释放三个端口。
4. 统一数据库drill与统一Temporal故障drill调用已有原测试；重复运行不重复累计独立场景数。
5. 新checkout冷缓存离线失败与温缓存冻结安装；现有quality总门禁、契约、迁移、测试、浏览器与文档全部保留。

本地开发环境Node22且无Docker；本地通过仅限实际运行的工具测试。CI采用仓库固定Node24.21.0/pnpm11.10.0。数据库和故障模型为合成测试，不是客户代码或live Agent。通过不等于生产可用或平台强制分支保护已配置。
