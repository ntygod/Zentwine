# ZT03-01 验证记录

状态：InProgress；最终验收以Issue #30与对应PR中的精确head/base、CI和制品核验为准，不预填成功结论。

本机Node22与已有TypeScript只完成定向类型及纯控制逻辑验证，不能替代固定Node24.21.0/pnpm11.10.0冻结依赖、真实PostgreSQL17.11、全部原浏览器与Temporal测试。固定Prettier3.6.2来自已校验的历史CI工具归档；未新增依赖或修改锁。

新测试覆盖真实RLS批量/关联/写入、缺上下文、GUC与search_path伪造、读写角色、访客拒绝、复合外键、运行账号不能关RLS/改函数、独立NOLOGIN迁移owner、连接池复用、异常及未等待异步回滚、会话/组织/成员/应急撤权、有效事务与撤权真实锁等待、时间失效最终重查及空表回退/数据保护。

原有测试、八项迁移演练流程、冻结依赖与只读CI权限保留。新增集成入口缺配置退出1。无新UI；旧实际浏览器仍必须回归。无真实模型/供应商调用、业务库迁移、生产部署；不是独立安全认证。

## 首轮失败与修复

初始head `0e82a048010f241df9757349e3e77aaef74a38a4`，CI `36217882185`。新31项PG测试在公共Fixture的运行角色权限自检失败（0/31），尚未进入隔离场景主体；原数据库回归通过，后续矩阵/浏览器/迁移被阻断，未合并。权限自检按完整表名解析身份表，但运行角色刻意没有身份schema的USAGE；修改为按系统目录OID检查表ACL，不增加运行权限、不删除检查。修复后的真实结果须由新head完整CI确认。
