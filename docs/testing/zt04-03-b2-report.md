# ZT04-03-B2 验证记录

状态：候选待完整目标环境验证；Issue #44。输入main `3b7014e74c431b50dafb4169cb2c49b5567bd5a6`。

## 范围和证据门禁

新增30项规则、加密游标、响应解码与控制器合成测试，本地与原目录审批30、导航21合计81/81。domain/policy/db/contracts/client本地类型编译通过。Node22.16.0/TypeScript5.8.3不是项目冻结Node24/pnpm11环境；实际PostgreSQL、Chromium、完整工程及持久性必须在该候选CI执行。本地仅从历史可核验制品提取冻结Prettier3.6.2用于格式检查，不执行其中业务代码，不以其失败轮次作为新功能证据。

19项新PG/HTTP包含本人/负责人隔离、资源read/deny在LIMIT前过滤、受限共享、大整数分页、新增记录界限、终态与过期、状态变化、会话/组织/筛选/权限游标拒绝、重启/篡改、撤权/应急/访客、实际TCP/错误返回以及真实数据库等待。8项新PG浏览器涵盖导航-详情衔接、独立审核人发现、换页/刷新、访客/隐藏记录、双窗口/撤权、离线/读取失败恢复、到期筛选及三主题/窄屏/键盘。2项默认浏览器只验证未启用与无效路由，不替代真实业务场景。

最终关联PR须记录准确head/base/candidate/tree、每个必需job、五份制品SHA和manifest条目、完整源码及页面实拍。遇失败先记录和修复再完整复验，不修改原测试或放宽权限，也不复用旧提交的绿色。

## 不计入完成的部分

发现列表不是持久正文版本、完整证据/活动/对话或通用内容批准。筛选的状态是数据库记录，期限只对未完成记录派生；批准/执行由B1重新核验。分页不承诺权限和状态的跨页历史一致性，不做生产规模性能认证。Issue #25、#32/#35及父Issue #40范围保留，不触碰受阻迁移校验。没有业务库迁移、生产部署、真实模型/IdP或付费动作。
