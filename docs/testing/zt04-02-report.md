# ZT04-02 验证记录

限定代码验收通过。本文记录代码快照，最终文档提交仍需独立完整CI，最终head、制品和合并以PR #39为准。Issue #38负责本工作包；Issue25/32/35既有验收与迁移阻断保持。

## 精确代码快照

输入main `91c05699aed5196b8d067e60e5ac133b26b9795f`；修复head `f5434124d2d20399b70afb88c0ad7788bbe8059c`；被测合并候选 `732145fb6a74d9503cfa7176c3f7b156d9bdad62`，parents为输入main和修复head；源码tree `48cda623e0fdb5c7d832908bb2a305d77c0e8b7b`。完整CI **36238790989**（run59、attempt1）成功，不拼接旧提交的部分通过结果。

## 实际回归

| 范围 | 结果 |
|---|---|
| 主工程 | 402/402（381既有＋21新契约/控制器测试） |
| 工作台/Studio浏览器 | 19/19（17既有＋2新默认模式/未知深链） |
| 组织真实Chromium＋PG | 12/12（4既有＋8新路由/授权/失效场景） |
| 审计、应急浏览器 | 各3/3 |
| 租户、组织、审计、应急PG/HTTP/WS | 34/34、53/53、28/28、26/26 |
| Agent、审批、策略、身份 | 51/51、49/49、38/38、28/28 |
| Fixture、隔离、迁移、独立环境、启动/取消 | 27/27、13/13、8/8、8/8、3/3 |
| Temporal | 8单元＋10集成 |
| 质量、契约 | 11Node＋39Python自测；6正例＋62负例 |
| 权限矩阵 | 24/24，12真实目录授权＋12预留入口防护 |

执行套件无失败、取消、skip、todo。重复Compose/统一数据库和持久性演练及清理通过，不把重复执行算新增覆盖。严格矩阵重复24项后仍因文件/预览/导出未实现退出2；缺配置退出1。真实模型独立skipped_live、executed=false。A矩阵remaining_deliverables是历史范围快照，不替代当前Issue的B/C状态。

新8项真实页面测试覆盖深链确认、刷新、负责人治理只读、访客目录隔离、菜单与直接API拒绝、延迟真实POST前清空、返回历史页不自动切换、另一窗口切换后复核、撤权后请求清空、断网及真实PG读取权限故障恢复。API和PG没有路由模拟；测试钩子仅延迟真实请求，不返回虚构授权响应。21项单测的合成传输明确区分于上述场景。

目标Node24.21.0/pnpm11.10.0冻结依赖、真实PG17.11和Chromium；格式/lint/构建/类型、冻结及离线安装、文档、八项迁移、源码不变和依赖审计通过。57旧保护测试文件/667声明、两个迁移校验器、八项历史迁移、工作流和锁保持。主工程锁SHA-256 `d2717174929acfbf9eb9dc7b0c171eeb5ba0fa5e3f33eb80b3bde41425f5c7fb`。

## 制品与实拍

五份归档SHA-256均与GitHub元数据匹配；**122个manifest条目**逐项核对长度/摘要，绑定上述candidate/tree/run/attempt且job_status=success。工程、数据库、文档三份完整源码包各**418文件**与目标Git树的路径、blob摘要和执行位一致。桌面负责人治理、访客目录与390px治理实拍已逐张检查，可读且窄屏无横向溢出。样例身份仅属于隔离测试，不在默认应用中展示为业务数据。

| 制品 | Artifact ID | SHA-256 |
|---|---|---|
| 质量 | 10905466288 | `8c9f78c80191dc96388ebe59658f769295a5af39c77757b150306013e837ef22` |
| 文档 | 10905436408 | `03e6af98050d3fa01148ac58b5069b82f99cd2801809ae4a2a80ec73a6a30ff5` |
| 工程 | 10905113018 | `4f061343a30ed37637385000538ddabfec758c125997cfb515659fdcfd2cb510` |
| 持久性 | 10905585949 | `5f63e8fa4a4db6122dd1be2f58e6efb0b49aa11da77f668b7bd0a7de17cae41e` |
| 数据库 | 10904883712 | `920ef56681a72cd5b5800abb2b1b5533a040409b96e5b4f9383e9fd4e4fdabec` |

摘要是完整性索引，不是独立认证。本表仅属于代码快照，最终文档提交的新制品另记于PR #39。

## 首轮失败与修复

初始head `88e40ec361c6c832176fcb6c7d68b75d837544b1`、tree `93c8a08cdc2b51f0ce704192ce9fea3f0d9ee973`、candidate `12618fb43ec5254714e13b43262a4ccfa250d8f6`，CI **36238460170**。402主工程及19默认浏览器通过，但组织UI只有5/12：原4项和新1项完成，其余7项在组织选择器隐式标签的精确查找处中止。虽然原生选择器已经显示，但精确标签查询无法匹配；下游审计/应急浏览器、迁移和环境未执行。没有据此合并。

修复只增加 `aria-label="切换到组织"`，保持可见标签、原生控件和全部断言/超时，未改权限、服务端或测试以掩盖失败。上述修复head重新从冻结安装执行完整CI，8个新真实场景才全部通过。首轮五份归档101个manifest及三份完整源码已核验，其数据库status=failure保留，不能当成功证据。

本机Node22.16.0的contracts/client编译、21项单测和固定Prettier只作定向检查，不替代目标环境结果。首轮上传核对发现任务状态文档一处空格不同，提交前使本地/远端索引一致；最终状态文档还原原有空格，无业务语义变化。

## 使用与验收边界

新入口依赖已有组织服务启用；未启用、未登录或无权限不展示虚构组织。新治理页只读，原组织控制台独立重读当前会话才执行管理动作。浏览器事件只使缓存失效，不授予权限。按次核验非实时订阅；持续可见且无新请求的旧快照不承诺远程撤回，终止页面/系统截图不可清除。静态宿主仍需正确文档回退与API分流，本次未部署生产代理。

不交付业务对象/项目/Run、Studio执行、数据库CAS、实时事件或新增模型适配。没有业务库迁移、生产部署、真实模型/IdP或付费动作，不重试Issue35被拦截的写入。按既有授权实施与自审，不称独立可访问性或安全认证。
