# ZT03-02 执行记录

整体状态：InProgress。A已通过PR #34合并；B在PR #33完成主线整合及适配层修复的候选，0009迁移兼容和真实持久性验收仍Blocked。Issue #32保持打开，不推进ZT03-03。

## A历史交付

A输入主线 `4cda38a41bcbf76856bc8c9a801525c9482037cd`，head `761f62b11b274a8e0e5a8c8486781f6af6496306`，完整CI `36223617006`。合并主线 `b7e1c4abd6a4f531e16e2dabf097dea2407e56e4`，tree `b9b9d31c983d95a6cc25bcd2f3db999377a3a84a` 与被测候选一致。370主工程、34既有租户PG及完整旧回归通过；这些34项不是B的32项版本PG。

A交付规范化、SHA-256、版本/关系/命令类型、有限状态规则及深度冻结命令快照；参见[开发说明](../development/version-primitives.md)与[ADR-019](../adr/ADR-019-version-primitives-increment.md)。A函数不连接数据库，不提供真实CAS或批准权限。

## B草稿整合与修复

原B head `9ba7a29aeaf58b569454a52413b2167c9b78ef07`，CI `36221177636` 实际failure：0009-up unsupported_migration_statement，后续完整回归未执行。兼容校验器的写入曾被工具拦截；本轮不重试该写入，也不修改SQL形式绕过检查。

本轮在保留B历史的同时纳入已合并A主线，不强推。保留A的规范化、摘要、黄金样本和26项测试，B直接使用snapshotRevisionCommand。旧versions模块的摘要入口只重导出同一content-digest实现，不再保留较弱的独立实现。db总入口对适配器采用显式导出，避免重复符号。

适配器修复：applied响应必须与命令描述符或状态动作的精确修订/摘要/目标状态一致；历史snapshot必须返回被请求的版本；异常谱系、重复返回行和错误关系拒绝返回；数据库响应无效统一视为服务错误，不能误报用户输入错误。仍保留外层最终鉴权、错误使事务失败、排空及回滚机制。适配层检查不能替代数据库授权和CAS。

本机Node22.16.0、TypeScript5.8.3定向类型编译通过；77/77单测包括A26、原B20、租户15及新增适配层16。新测试使用明确标注的合成SQL响应，不声称执行PostgreSQL。原B20项测试文件与原稿字节相同，32项版本PG定义也不修改。完整目标环境结果以PR #33当前head为准，不沿用A的绿色。

0009 up/down/verify和manifest、原两份迁移校验器、八项历史迁移、CI工作流、冻结依赖及旧测试保持各自原有内容。当前没有解除Draft或合并数据库候选，没有新增绕过门禁的执行入口。下一必要条件仍是由维护者独立审查并提交迁移校验兼容修复，随后真实执行32项版本PG和全量回归；在此之前不标记B完成。

Issue #25的文件/预览/导出完整通道待办继续保留。无业务库迁移、生产部署、真实模型/IdP或付费操作。
