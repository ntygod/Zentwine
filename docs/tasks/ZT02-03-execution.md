# ZT02-03 执行记录

任务：Agent身份、责任人、父子授权、期限与PolicySnapshot。基线`main@0edf8dea0ab7e4a2f319c8dad9a089788a7713b4`，Issue #19，分支`feat/ZT02-03-agent-delegation`。

状态：InProgress，待最终精确提交CI和制品核验。按项目负责人既有授权实施、自审及合并；没有第三方独立审核声明。

## 交付

- 纯收窄规则、身份和授权端口，不把模型角色等同人类身份。
- 0003迁移和控制面仓储，精确资源/动作/环境；八层授权、额度预留及消费。
- 不可改写快照、始终保留责任人与祖先；根撤销阻断后续调用。
- 人类签发/停用/撤销入口与Agent凭据/子授权/工具入口，默认关闭。
- request_id幂等、失败回滚、权限和锁等待复核，控制面数据库不交给Agent。
- 规则/API及真实PG、loopback HTTP验证，纳入原有统一门禁。

预算单位是成功目录工具调用，不是模型费用。保守不退款、组织级串行和策略版本更新使整棵树失效的代价明确记录于[ADR-013](../adr/ADR-013-agent-delegation.md)。

## 下一项

ZT02-04：版本绑定审批、职责分离、短期动作许可与撤权通知。未通过本任务代替该项验收。
