# ZT02-02｜统一授权内核执行记录

输入 main@979ec37761f52390c938297de63cbcabffb2c0ad，Issue #17。状态：Done（限定实现验收）。首轮完整CI已通过；本记录后的精确复验与合并以PR #18为准。决定参见 [ADR-012](../adr/ADR-012-authorization-policy.md)。

交付：policy 纯规则、策略版本与到期、精确角色/授权、资源目录 0002 迁移、受控读取/改名、HTTP/工具/真实WS共用执行入口、权限修改与执行之间的事务锁、操作员入口、测试和开发说明。

目标Node24.21.0 / pnpm11.10.0首轮完整CI通过：174主工程（原145+新29）；新增24个仓储/HTTP/工具场景（含1模拟连接故障）和14个真实TCP WS场景；原28身份PG、9浏览器、开发环境、迁移和持久故障检查保留通过。详见[验证报告](../testing/zt02-02-report.md)。首个工作流仅准备固定依赖，contents:read，无secret和ref写入；最终树不含临时作业。

未知动作默认拒绝，needs_approval 不执行。服务端重新读取权威身份、成员、策略、资源和到期状态；反向依赖和客户端角色伪造拒绝。显式授权是精确动作，不支持通配符；单资源读权限是公共评估/改名的前置。

验收证据必须包含策略单测、真实PG锁/事务、WS握手/消息撤权、原回归、迁移和制品摘要。当前引用的外部依赖ws8.21.3来自官方发布及已核对registry integrity，不冒称所有第三方缺陷均可排除。

范围：不完成整个ZT02、Agent/服务身份、审批签发、业务工作区、真实多模型或生产认证。后续ZT02-03。回退先关闭policy模式，数据保留；开发数据销毁的down需显式执行。
